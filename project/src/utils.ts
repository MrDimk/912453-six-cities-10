export const ratingRate = (rating: number): string => `${rating / 5 * 100}%`;

// export const addKeyPressListener(cb)

// export const keyPressHandler = (evt: KeyboardEvent) => {
//   if(evt.key === 'Esc') {

//   }
// };
